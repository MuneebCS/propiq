import 'dart:ui';

class CustomColors {
  static const primary = Color(0xFFFFFFFF);
  static const secondary = Color(0xFF07A5CB);
  static const error = Color(0xFFF4041F);
  static final Color overlayBackground = primary.withAlpha((0.2 * 255).toInt());
}
